{% extends "admin/base.html.twig" %}

{% block title %}Modifier un utilisateur
{% endblock %}


{% block body %}
	<main class="container">
		<h1 class="mb-3">Mettre a jour un utilisateur</h1>

		{{ form_start(userForm)}}
		{{ form_rest(userForm)}}

		<input type="submit" value="Modifier" class="btn btn-primary">
		{{ form_end(userForm)}}
	</main>
{% endblock %}
